@model IEnumerable<BookingForm.Models.Floor>
@{
    ViewData["title"] = "Floor";
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apartments)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Sample" asp-route-str="@item.ImagePath">Xem</a>
            </td>
            <td>
                @if (item.Apartments != null && item.Apartments.Count > 0)
                {
                    <a asp-action="GetApartments" asp-route-id="@item.Id">Danh sách căn hộ</a>                
                    
                }
            </td>
        </tr>
}
    </tbody>
</table>